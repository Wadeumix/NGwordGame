# NGワードゲーム 起動方法

## 一番かんたん：ダブルクリックで起動
このフォルダの **`ゲーム起動.command`** をダブルクリックするだけ。
ターミナルが開いてサーバとトンネルが立ち上がり、共有用URL（`https://〇〇〇.trycloudflare.com`）が表示されます。
そのURLを友達に送れば一緒に遊べます。

> 初回だけ「開発元を確認できないため開けません」と出ることがあります。
> その場合は `ゲーム起動.command` を **右クリック →「開く」→「開く」** で許可してください（次回からはダブルクリックでOK）。

遊び終わったら、開いたターミナルのウインドウで **`Ctrl + C`** を押すか、ウインドウを閉じれば停止します。

---

## 手動でやる場合（コピペ用）

### ① ゲームサーバを起動（1枚目のターミナル）
```sh
export PATH="$HOME/.local/node/bin:$PATH"
cd "/Users/n00025/Desktop/ClaudeMade/NGワードゲーム"
node server.js
```
`NGワードゲーム: http://localhost:3000` と出たら成功。この画面は閉じない。

### ② 公開トンネルを起動（2枚目のターミナル、⌘+N で新規ウインドウ）
```sh
~/.local/bin/cloudflared tunnel --url http://localhost:3000
```
表示された `https://〇〇〇.trycloudflare.com` を友達に共有。

---

## メモ
- **Macは付けっぱなし**が必要（サーバが自分のMacで動いているため）。
- 公開URLは起動するたびに変わります。**遊ぶたびに最新のURLを共有**してください。
- 同じWi-Fi内だけで遊ぶなら、トンネル無しで `http://localhost:3000`（自分）や
  `http://<MacのIP>:3000`（同じWi-Fiの人）でもアクセスできます。
  MacのIPは `ipconfig getifaddr en0` で確認。
